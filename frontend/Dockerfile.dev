# Development Dockerfile with hot reloading
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code (will be overridden by volume mount, but needed for initial build)
COPY . .

# Expose Vite dev server port
EXPOSE 3000

# Start Vite dev server (host binding already configured in vite.config.ts)
CMD ["npm", "run", "dev"]

